name: Update KOSPI200 dashboard

on:
  schedule:
    # cron은 UTC 기준. 한국시간(KST=UTC+9) 평일 08:00~20:50, 10분 간격.
    - cron: "*/10 23 * * 0-4"    # KST 08:00~08:50 (월~금)
    - cron: "*/10 0-11 * * 1-5"  # KST 09:00~20:50 (월~금)
  workflow_dispatch: {}       # 수동 실행 버튼

permissions:
  contents: write

concurrency:
  group: update-dashboard
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run screener
        run: python src/main.py

      - name: Commit dashboard
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/index.html
          if git diff --cached --quiet; then
            echo "변경 없음 - 커밋 생략"
          else
            git commit -m "chore: 대시보드 자동 갱신 ($(date -u +%Y-%m-%dT%H:%MZ) UTC)"
            git push
          fi
